# k8s-security-audit-cli
A Python-based CLI tool to audit Kubernetes clusters for common security risks such as open access, insecure RBAC, public services, and privileged containers. Uses the Kubernetes Python client to access real-time cluster state.
